// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   output: "export",
//   distDir: "./dist",
// };

// export default nextConfig;

/** @type {import('next').NextConfig} */
const nextConfig = {
  distDir: "./dist", // Relative to the .next directory
};

export default nextConfig;
