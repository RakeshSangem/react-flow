// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   output: "export",
//   distDir: "./dist",
// };

// export default nextConfig;

/** @type {import('next').NextConfig} */
const nextConfig = {
  distDir: "build", // Relative to the .next directory
};

export default nextConfig;
